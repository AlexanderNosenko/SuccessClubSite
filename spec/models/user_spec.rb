require 'rails_helper'

RSpec.describe User, type: :model do
  pending "add some examples to (or delete) #{__FILE__}"
  # before(:all) do
  # 	@user = User.create(name: 'Da', last_name: 'nil', password: 'passpass')
  # end
  # it 'has to change wallet main balance after give and take'

  # it 'has to have wallet' do
  # 	expect(@user.wallet).to be_an(Wallet)
  # end

  # after(:all) do
  # 	@user.destroy!
  # end
end
